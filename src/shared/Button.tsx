import { styled } from 'styled-components'

const Wrapper = styled.button`
  color: white;
  cursor: pointer;
  width: 400px;

  line-height: 1.4;
  border-radius: 10px;
  background-color: royalblue;
`

export const Button = () => {
  return <Wrapper>dfdf</Wrapper>
}
